#import "../template.typ":*
#import "@preview/mitex:0.2.6": *

= Теоремы Геделя и пиздец больного ублюдка



#def[def:] Теория называется *ω-непротиворечивой*, если для любой формулы $φ(x)$ из того, что доказуемы $φ(0)$, $φ(1)$, $φ(2)$, ... (т.е. $φ(n)$ для всех натуральных $n$), следует, что не доказуемо $∃x.¬φ(x)$.

#def[def:] Определим формулу $𝜎(𝑥_1) := ∀𝑝.¬𝜔_1(𝑥_1, 𝑝).$

#theorem(name:[Первая теорема Гёделя о неполноте арифметики], body:[
 -   Если формальная арифметика непротиворечива $tack.r.not sigma(⌜sigma⌝)$
- Если формальная арифметика ω-непротиворечива  $tack.r.not not sigma(⌜sigma⌝)$
], proof:[
  Напомним $sigma (x_1):= forall p. not w_1(x_1,p)$

  1. Пусть $tack.r sigma(⌜sigma⌝)$. Тогда есть $p$ - номер доказательства, тогда $tack.r w_1(⌜sigma⌝, p)$. Тогда $tack.r exists p. w_1(⌜sigma⌝, p)$, тогда $tack.r not sigma(⌜sigma⌝)$. Противоречие
  2. Пусть $tack.r sigma(⌜sigma⌝)$. То есть $exists p. w_1 (⌜sigma⌝,p)$
    - Но найдется ли натуральное число p, что $tack.r w_1(⌜sigma⌝, p)$? Пусть нет. Тогда по $w$-непротиворечивости $tack.r.not exists p. not not w_1 (⌜sigma⌝, p)$.  То есть $p$ доказательство самоприменения: $tack.r omega(⌜sigma⌝)$. Противоречие

    TODO: что что что?
])

#def[def:] Теория называется *синтаксически полной*, если для любой замкнутой формулы α этой теории верно либо ⊢ α, либо ⊢ ¬α.

#def[def:]  Теория называется *семантически полной* (относительно своей стандартной модели), если любая формула, истинная в стандартной модели, доказуема в теории.

#theorem(body:[
  Формальная арифметика (при условии её непротиворечивости) не является ни синтаксически, ни семантически полной. Существуют формулы, которые неразрешимы (не доказуемы и не опровержимы), и существуют истинные в стандартной модели формулы, которые недоказуемы.
], proof:[
  Рассмотрим Ф.А. с классической моделью. Из теоремы Гёделя имеем ̸⊢ 𝜎(⌜𝜎⌝). Рассмотрим
𝜎(⌜𝜎⌝) ≡ ∀𝑝.$¬𝜔_1(⌜𝜎⌝, 𝑝)$: нет числа 𝑝, что 𝑝 — номер доказательства 𝜎(⌜𝜎⌝). То есть, $bsl forall p. not w_1(⌜sigma⌝,p) bsr$. То есть $models sigma(⌜sigma⌝)$ 

])

#def[def:] Пусть $chevron.l ⌜xi⌝ chevron.r in W_2$, если $tack.r not xi(⌜xi⌝)$ и $p$ номер доказательства. Пусть $w_2$ выражает $W_2$ в формальной арифметике.

#theorem(name:[Первая теорема Геделя в форме Россера],body:[
  Рассмотрим $p(x) = forall p. w_1(x_1,p) -> exists q.q <= p \& w_2 (x_2 q)$. Тогда
  $tack.r.not p(⌜p⌝)$ и $tack.r.not not p(⌜p⌝)$. Неформальный смысл: "Меня легче опровергнуть, чем доказать"
])

#def[def:] Теория первого порядка, использующая нелогические функциональные символы $0$, $(+)$ и $(\cdot)$, нелогический предикатный символ $(=)$ и следующие нелогические аксиомы, называется *системой Робинсона*.
#mitex(`
$$\begin{array}{ll}
a = a & a = b \rightarrow b = a \\
a = b \rightarrow b = c \rightarrow a = c & a = b \rightarrow a' = b' \\
a' = b' \rightarrow a = b & \neg 0 = a' \\
a = b \rightarrow a + c = b + c \& c + a = c + b & a = b \rightarrow a \cdot c = b \cdot c \& c \cdot a = c \cdot b \\
\neg a = 0 \rightarrow \exists b. a = b' & a + 0 = a\\
a + b' = (a + b)' & a \cdot 0 = 0 \\
a \cdot b' = a \cdot b + a 
\end{array}$$
`)

Система Робинсона неполна: аксиомы --- в точности утверждения, необходимые для доказательства теорем Гёделя. Система Робинсона не имеет схем аксиом.

#def[def:] Теория первого порядка, использующая нелогические функциональные символы $0$, $1$, $(+)$, нелогический предикатный символ $(=)$ и следующие нелогические аксиомы, называется *арифметикой Пресбургера*.

#mitex(`
$$\begin{array}{l}
\neg (0 = x + 1) \\
x + 1 = y + 1 \rightarrow x = y\\
x + 0 = x \\
x + (y + 1) = (x + y) + 1\\
(\varphi(0) \& \forall x.\varphi(x) \rightarrow \varphi(x+1)) \rightarrow \forall y.\varphi(y)
\end{array}$$
`)

#def[Теорема.] Арифметика Пресбургера разрешима и синтаксически и семантически полна.



/*
### IV. Полнота и неполнота

5.  Синтаксическая полнота: Теория называется синтаксически полной, если для любой замкнутой формулы α этой теории верно либо ⊢ α, либо ⊢ ¬α.
6.  Семантическая полнота: Теория называется семантически полной (относительно своей стандартной модели), если любая формула, истинная в стандартной модели, доказуема в теории.
7.  Следствие из первой теоремы Гёделя: Формальная арифметика (при условии её непротиворечивости) не является ни синтаксически, ни семантически полной. Существуют формулы, которые неразрешимы (не доказуемы и не опровержимы), и существуют истинные в стандартной модели формулы, которые недоказуемы.

### V. Расширения формальной арифметики

8.  Неполнота расширений: Любое непротиворечивое, рекурсивно аксиоматизируемое расширение формальной арифметики также является неполным (в нём будут свои неразрешимые формулы). Это означает, что добавление конечного числа новых аксиом или схем аксиом, которые можно эффективно распознать, не может сделать арифметику полной.

### VI. Ослабленные арифметические системы

9.  Система Робинсона (Q): Конечная аксиоматизация теории, содержащей базовые свойства сложения, умножения, порядка и индукцию только для формул специального вида (отсутствует полная схема индукции). Система Робинсона достаточно сильна для доказательства теорем Гёделя, а значит, является неполной.
10. Арифметика Пресбургера: Теория натуральных чисел с операцией сложения (но без умножения), включающая схему индукции. Арифметика Пресбургера является разрешимой, синтаксически и семантически полной. Это показывает, что неполнота и неразрешимость существенно связаны с наличием умножения.

### VII. Дополнительные ключевые теоремы (из конспекта)

11. Вторая теорема Гёделя о неполноте: Если формальная арифметика непротиворечива, то формула Consis, формально выражающая её непротиворечивость, недоказуема в самой этой арифметике. (Формально: если ⊢ Consis, то теория противоречива).
12. Условия выводимости (Гильберта-Бернайса-Лёба): Набор условий, которым должна удовлетворять формула π(x), выражающая отношение "x есть номер доказуемой формулы". Эти условия используются при строгом доказательстве теорем Гёделя.
    *   Если ⊢ α, то ⊢ π(⌜α⌝).
    *   ⊢ π(⌜α → β⌝) → π(⌜α⌝) → π(⌜β⌝).
    *   ⊢ π(⌜α⌝) → π(⌜π(⌜α⌝)⌝).
13. Теорема Тарского о невыразимости истины: Не существует формулы φ(x) языка формальной арифметики такой, чтобы для любой формулы ψ было верно: ψ истинна в стандартной модели тогда и только тогда, когда φ(⌜ψ⌝) истинна в стандартной модели. Иными словами, понятие истинности для арифметики невыразимо в её же языке.

*/